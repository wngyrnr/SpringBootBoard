<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tester.boardinsert.BoardMapper">

<select id="findAll" resultType="com.example.tester.boardinsert.BoardInsertApi">
    SELECT board_id, category, title, created_at
    FROM board_table
    ORDER BY created_at DESC
</select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="boardId">
        INSERT INTO board_table (writer, category, title, content)
        VALUES (#{writer}, #{category}, #{title}, #{content})
    </insert>

</mapper>