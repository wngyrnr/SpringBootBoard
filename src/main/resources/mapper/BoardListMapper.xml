<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tester.boardList.mapper.BoardListMapper">

<!--
    list로 반환받고 싶을 때
    Mapper의 메서드의 리턴 타입을 list<dto함수명>로 선언
    sql이 여러행을 반환할 때 MyBatis가 각 행마다 BoardListDto 인스턴스를 자동으로 리스트에 담음
-->
    <sql id="searchCondition">
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'writer'">
                    writer LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    (title LIKE CONCAT('%', #{keyword}, '%')
                    OR content LIKE CONCAT('%', #{keyword}, '%')
                    OR writer LIKE CONCAT('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </if>
    </sql>

    <select id="findByPage" resultType="com.example.tester.boardList.dto.BoardListDto">
        SELECT category, title, writer, created_at
        FROM board_table
        <where>
            <include refid="searchCondition"/>
        </where>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM board_table
        <where>
            <include refid="searchCondition"/>
        </where>
    </select>



</mapper>