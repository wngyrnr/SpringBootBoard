<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tester.boardupdate.mapper.BoardUpateMapper">

    <update id="update">
        UPDATE board_table
        SET writer = #{writer}, category = #{category}, title = #{title}, content = #{content}
        WHERE board_id = #{boardId}
    </update>

    <select id="findselect" resultType="com.example.tester.boardupdate.dto.BoardUpdateDto">
        SELECT board_id,category, title, writer, content, created_at
        FROM board_table
        WHERE board_id = #{boardId}
    </select>


</mapper>
